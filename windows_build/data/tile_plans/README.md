# Tile plan cache (`v1/`)

Same as `scripts/data/tile_plans/README.md`. Build caches from the **Linux `scripts/`** tree with
`build_tile_plan_cache.py`, then copy `*.tiles.gz` here if you ship a Windows bundle that includes
precomputed plans.
